# 开发路径

本文记录首版交付范围和后续可迭代方向。原则是先让核心体验跑通，再逐步提高完成度。

## 方向

技术路线：

- Flutter。
- 首版 Android APK。
- 后续保留 iOS/iPadOS 移植。
- 表盘采用可换皮 Dial Renderer：首版代码绘制，后续可接入定制美术资源。

产品方向：

- 极简机械表盘番茄钟。
- 无文字、无数字、无设置页。
- 用户只做两个动作：拖动绿色扇形边缘设定时间，点击中心无文字按钮开始或结束。

## 首版

首版目标是做出一个前台可用、可打包 APK 的练手作品。

功能：

- 竖屏单页面。
- 默认 5 分钟。
- 可设定 5-120 分钟。
- 1 分钟步进。
- 一圈 60 分钟，允许第二圈。
- 绿色扇形从 12 点方向顺时针铺到设定边缘。
- 没有实体指针，用户拖动扇形结束边缘。
- 扇形结束边缘有轻微阴影带。
- 中心无文字按钮用于开始和结束。
- 运行中扇形按剩余时间逆时针退回。
- 运行中点击中心按钮立即结束并重置为 5 分钟。
- 完成后提示音和震动，然后重置为 5 分钟。

不做：

- 后台或锁屏提醒。
- 系统通知。
- 设置页。
- 声音/震动开关。
- 暂停。
- 休息、长休息、循环轮次。
- 数据统计。
- 账号。
- 文字引导。
- iOS 打包。

## 首版工程步骤

1. 初始化 Flutter 项目。
2. 建立基础目录结构。
3. 实现 `DialGeometry`，完成角度、分钟、两圈范围和边缘命中区逻辑。
4. 为 `DialGeometry` 写单元测试。
5. 实现 `DialTimerState`，完成 `idle` 和 `running` 状态。
6. 为计时状态写单元测试。
7. 实现基础 `DialRenderer`，先用代码绘制表盘、刻度、扇形、边缘阴影和中心按钮。
8. 实现 `DialTimerPage`，连接手势和状态。
9. 实现前台提示音、完成震动和拖动触觉反馈。
10. 添加基础 widget 测试。
11. 在 Android 模拟器或真机运行。
12. 打包调试 APK。
13. 做一次代码检查和完成前验证。

## 后续迭代

视觉打磨：

- 使用定制表盘外壳资源。
- 使用定制表盘底纹。
- 使用定制五分钟刻度。
- 使用定制中心按钮。
- 为绿色扇形增加纹理、材质和更自然的边缘阴影。

移动端完整性：

- Android 后台/锁屏提醒。
- Android 通知权限。
- Android 精确闹钟权限。
- 不同 Android 品牌后台策略的说明或引导。

跨平台：

- iOS/iPadOS 打包。
- iOS 通知、震动和音频行为适配。
- iPad 大屏布局。

质量：

- 更完整的 widget 测试。
- 视觉截图测试。
- 真机触控手感测试。
- 性能检查，确保表盘动画和重绘稳定。

